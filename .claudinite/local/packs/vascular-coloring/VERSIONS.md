# Version history

A local pack is neither versioned nor distributed. This file carries one row per change automatic
work makes to it — a prose rule added, stripped to its residue, or removed as canon-covered.

| Date | Task | Change |
|---|---|---|
| 2026-08-24 | `growth-dedup` | Stripped **Raw length is not comparable across figures** to its residue (**Cross-paper comparisons are never averaged**) — the normalize-don't-average-raw-quantities instruction is now `research-project` RULES.md §"A raw measurement is comparable only inside one calibration" ("Aggregate and compare only normalized quantities... never a mean spanning two sources that differ in more than scale... mark it uncalibrated with the reason"); kept only the paper-specific magnitude fact and the grouping key. |
| 2026-08-24 | `growth-dedup` | Trimmed **Say which numbers are trustworthy**'s closing instruction ("say so where it is quoted") — covered by `research-project` RULES.md §"Show, don't just tell" ("State that boundary inline, beside the number, not in a separate caveats section"); kept only the list of which quantities in this pipeline are/aren't trustworthy. |
