# implement-request worker — moved

These instructions now live at **`public/implement-request.md`**, two directories up from
this one (`.claudinite/shared/packs/claudinite-tasks/public/implement-request.md` in a member,
`packs/claudinite-tasks/public/implement-request.md` in the canon).

**Read that file and follow it.** It is the whole of this task's spec; nothing here adds to it.

---

This path is kept only for work items minted before the move, whose machine block names it —
an issue body is not a file this repository can rewrite. Every item minted since names the
public path directly. It is removed once the last such item has converged, one month from
2026-09-15.
